// Background effect (already styled in CSS)
document.addEventListener("DOMContentLoaded", () => {
  const bg = document.querySelector(".background-animation");
  if (bg) {
    // Extra effects can be added here later
  }
});
